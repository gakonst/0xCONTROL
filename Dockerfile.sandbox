FROM docker.io/cloudflare/sandbox:0.4.18

RUN apt-get update \
  && apt-get install -y --no-install-recommends ffmpeg \
  && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir yt-dlp==2024.11.4

WORKDIR /workspace
